# Best Practices

Guidance for both script authors and users of curl|bash installers.

- [Invocation Styles](invocation.md) — comparing `curl | bash`, `curl -o- | bash`, and command substitution patterns
